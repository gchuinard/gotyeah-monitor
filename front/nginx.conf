server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # N'expose pas la version de nginx (Server + corps des pages d'erreur) -> évite
    # de donner une cible de version précise (CWE-497).
    server_tokens off;

    # Compression des assets texte
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css application/javascript application/json image/svg+xml;

    # En-têtes de sécurité, appliqués à TOUTES les réponses. NB nginx : une location qui
    # déclare son propre `add_header` n'hérite PLUS de ceux-ci -> on utilise `expires`
    # (et non add_header) pour le cache des assets, afin de garder ces en-têtes partout.
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "DENY" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Cross-Origin-Opener-Policy "same-origin" always;
    add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;
    # CSP stricte : script-src SANS 'unsafe-inline', avec les hashes SHA-256 des 2 scripts
    # inline (thème anti-flash + bootstrap SvelteKit). Le placeholder de script-src est
    # remplacé AU BUILD par inject-csp-hashes.cjs (hashes extraits de build/index.html → toujours
    # synchro avec le HTML servi, et créditée par les scanners car en HEADER). frame-ancestors
    # (header-only) couvre le clickjacking ; style-src garde 'unsafe-inline' (styles Svelte).
    add_header Content-Security-Policy "default-src 'self'; script-src 'self' __CSP_SCRIPT_HASHES__; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self' data:; connect-src 'self' https://api-monitor.gautierchuinard.com; object-src 'none'; base-uri 'self'; frame-ancestors 'none'; form-action 'self'" always;

    # Assets immuables versionnés par Vite (hash dans le nom) : cache long via `expires`
    # (pas add_header) pour HÉRITER des en-têtes de sécurité ci-dessus.
    location /_app/immutable/ {
        expires 1y;
        try_files $uri =404;
    }

    # SPA : toute route inconnue (y compris /edit/:id) retombe sur le shell client.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
